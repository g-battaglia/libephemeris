# LibEphemeris 3.0.0rc6

**Release date:** 2026-07-10
**Author:** Giacomo Battaglia
**License:** Apache-2.0
**Python:** >= 3.12
**Status:** Release candidate of the v3.0.0 line, published on the PyPI
pre-release channel; promoted to `3.0.0` final unchanged if it proves clean.

---

## Headline

> **rc6 is a documentation, provenance-disclosure and packaging polish pass.**
> No public API or numeric behaviour changed.

## What changed

**Superseded provenance approach.** This candidate documented legacy
output-derived compatibility data. That approach was retired in the subsequent
clean-room review: unsupported fictitious rows and lunar fitted artifacts were
removed and replaced, where possible, by independently published elements,
ERFA/IERS arguments, and direct JPL state geometry. No values from the retired
artifacts are reproduced here.

**Accurate dependency claims.** The ΔT-model and dependency-licensing wording was corrected: the
default `smh2016` ΔT is obtained from Skyfield (MIT), only `espenak_meeus`
is a native reimplementation; the core carries no strong-copyleft
(GPL/LGPL/AGPL) runtime dependency, with certifi's weak MPL-2.0 excepted and
disclosed.

**Licensing surface.** `libephemeris[all]` is permissive-only — the GPL-3.0
`rebound`/`assist` N-body packages are pulled only by the explicit opt-in
`[nbody]` extra.

## Compatibility & quality

- Public API unchanged; 1:1 reference parity preserved (both test backends
  green at 16,024 tests each).
- `provenance:sweep`, `license:check` and `wheel:audit` gates pass.

## Install

```bash
pip install --pre libephemeris==3.0.0rc6
```
