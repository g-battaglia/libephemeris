# LibEphemeris v1.0.0-alpha.2 — LEB2 Compressed Ephemeris Format

**Release date:** 2026-03-26

**Status:** Unpublished source milestone; no matching PyPI artifact exists.

---

## Highlights

This release introduces **LEB2**, a compressed binary ephemeris format that reduces file
sizes by 5-15x while maintaining sub-arcsecond precision. The core body set (14 bodies
covering all planets, lunar nodes, and Mean Apogee) compresses to **7.7 MB** — small
enough to ship directly inside a PyPI wheel. This is the foundation for making
`pip install libephemeris` work out of the box with no additional downloads.

---

## LEB2: How It Works

### The Problem

LEB1 files store Chebyshev polynomial coefficients as raw float64 values. The
early monolithic files were too large for ordinary wheel distribution. Standard
lossless compression also performs poorly because IEEE 754 mantissa bits appear
random to general-purpose compressors. Historical measurements for subsequently
retired unsupported-hypothetical groups are not retained here.

### The Solution: Error-Bounded Lossy Compression

LEB2 exploits a key insight: **most Chebyshev coefficients carry far more precision than
needed**. For a degree-13 polynomial fit of the Moon's trajectory:

| Coefficient | Magnitude | Bits needed for 0.001" | Bits in float64 |
|---|---|---|---|
| c0 (mean position) | ~1.0 AU | 28 | 52 |
| c1 (linear rate) | ~0.01 AU | 23 | 52 |
| c5 (5th order) | ~1e-8 AU | 4 | 52 |
| c6-c13 (high order) | < 5e-9 AU | **0** (noise floor) | 52 |

57% of the Moon's coefficients are below the precision target and can be zeroed entirely.
The remaining coefficients only need a fraction of their 52 mantissa bits.

### Compression Pipeline

```
Raw coefficients (float64)
    |
    v
[1] Mantissa truncation — zero out unneeded mantissa bits per coefficient order
    |
    v
[2] Coefficient-major reorder — group same-order coefficients across all segments
    |                           (all c0 together, all c1 together, ...)
    v
[3] Byte shuffle — transpose byte lanes (HDF5/Blosc-style) so exponent bytes
    |               and zeroed mantissa bytes cluster together
    v
[4] zstd level 19 — the regularized data now compresses excellently
    |
    v
Compressed blob (~6-15x smaller)
```

### Measured Results (Base Tier)

| Group | Bodies | LEB1 | LEB2 | Ratio |
|---|---|---|---|---|
| **Core** | Sun-Pluto, Earth, nodes, Mean Apogee | 44.5 MB | **7.7 MB** | **7.3x** |
| Asteroids | Chiron, Ceres, Pallas, Juno, Vesta | 23.0 MB | 7.66 MB | 4.0x |
| Apogee | Oscu/Interp Apogee, Interp Perigee | 31.6 MB | 10.31 MB | 3.8x |

### Precision Verification

All 14 core bodies verified against LEB1 reference at 500 random dates (1850-2150):

| Body | Max error vs LEB1 | Target |
|---|---|---|
| Sun | 0.0009" | < 0.001" |
| Moon | 0.0013" | < 0.001" |
| Mercury | 0.0022" | < 0.001" |
| Venus | 0.0025" | < 0.001" |
| Mars | 0.0014" | < 0.001" |
| Jupiter | 0.0007" | < 0.001" |
| Saturn | 0.0010" | < 0.001" |
| Uranus | 0.0010" | < 0.001" |
| Neptune | 0.0007" | < 0.001" |
| Pluto | 0.0008" | < 0.001" |
| Earth | 0.0014" | < 0.001" |
| Mean Node | 0.0006" | < 0.001" |
| True Node | 0.0011" | < 0.001" |
| Mean Apogee | 0.0008" | < 0.001" |

Errors are within or very close to the 0.001" target. These are errors **relative to LEB1**,
not absolute — the underlying Chebyshev fit error vs Skyfield/DE440 is additive on top.

---

## Modular File Architecture

LEB2 introduced a modular packaging strategy. The historical
unsupported-hypothetical companion was retired during the clean-room review;
prebuilt asteroid and apogee companions are no longer distributed either:

```
data/leb2/
  base_core.leb         7.7 MB   (14 bodies — PyPI wheel)
  base_asteroids.leb    locally generated from JPL data
  base_apogee.leb       locally generated from independent models
```

The `CompositeLEBReader` auto-discovers companion files:

```python
import libephemeris as swe

# Set path to any group file — companions are auto-discovered
swe.set_leb_file("data/leb2/base_core.leb")
swe.set_calc_mode("leb")

# Bodies from different files work transparently
swe.swe_calc_ut(2451545.0, swe.SE_SUN, swe.SEFLG_SPEED)      # from core
swe.swe_calc_ut(2451545.0, swe.SE_CHIRON, swe.SEFLG_SPEED)    # from asteroids
```

---

## LEB Parameter Optimization

Independent of the LEB2 format, Chebyshev fitting parameters were optimized for
independently sourced bodies. Historical optimization data for unsupported
hypothetical IDs was removed with those models.

- **Pluto:** interval 32d/degree 13 → 64d/degree 11. 0.0005" fitting error. Saves 0.6 MB.

All other bodies were tested and documented in `proposals/leb-optimization-findings.md`.
Notable findings: Mercury (e=0.206) and asteroids cannot tolerate wider intervals due
to orbital eccentricity; Jupiter/Saturn have mutual perturbations requiring high degree.

---

## Poe Task Reference

```bash
# Convert LEB1 -> LEB2
poe leb2:convert:base                # All groups
poe leb2:convert:base:core           # Core only (~6.5 MB)
poe leb2:convert:base:asteroids      # Asteroids only
poe leb2:convert:base:apogee         # Apogee variants only
poe leb2:convert:medium              # Medium tier (all groups)
poe leb2:convert:extended            # Extended tier (all groups)

# Verify
poe leb2:verify:base                 # Compare LEB2 vs LEB1

# Test
poe test:leb2                        # 27 unit tests
```

---

## New Files

| File | Lines | Purpose |
|---|---|---|
| `libephemeris/leb_compression.py` | ~200 | Compression/decompression primitives |
| `libephemeris/leb2_reader.py` | ~260 | LEB2 reader with lazy decompression |
| `libephemeris/leb_composite.py` | ~190 | Multi-file composite reader |
| `scripts/generate_leb2.py` | ~770 | CLI: convert, convert-all, generate, verify |
| `scripts/sweep_leb_params.py` | ~380 | Parameter optimization sweep tool |
| `tests/test_leb/test_leb_compression.py` | ~150 | Compression test suite |
| `tests/test_leb/test_leb2_reader.py` | ~240 | Reader + composite test suite |

## Modified Files

| File | Change |
|---|---|
| `libephemeris/leb_format.py` | LEB2 constants, CompressedBodyEntry, serialization |
| `libephemeris/leb_reader.py` | `open_leb()` factory (auto-detect LEB1/LEB2) |
| `libephemeris/state.py` | LEB2 discovery, CompositeLEBReader integration |
| `libephemeris/context.py` | Use `open_leb()` |
| `libephemeris/download.py` | Use `open_leb()` |
| `libephemeris/fast_calc.py` | TYPE_CHECKING for LEB2Reader |
| `pyproject.toml` | zstandard dep, version bump, poe tasks |

---

## What's Next (1.0.0a3)

- Ship `core_base.leb` inside the PyPI wheel for zero-config out-of-the-box experience
- Auto-load bundled LEB2 file when no other ephemeris is configured
- Document local generation for non-bundled LEB2 files
- Horizons live backend for zero-install experience (no local files at all)
